<!-- 
	El FrameLayout permet sobreposar elements de forma sencilla, necessari per
	poder mostrar el menú d'opcions.
 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="top"
    tools:context=".MainFragment" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content" >

            <ImageView
                android:id="@+id/splash_titol"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@android:drawable/ic_menu_search" />

            <EditText
                android:id="@+id/main_search_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginRight="30dp"
                android:ems="10"
                android:hint="@string/main_search_hint"
                android:text=""
                android:textColorHint="@color/semitransparent_white"
                android:textColor="@color/white"
                android:lines="1"
                android:maxLines="1"
                android:singleLine="true"
                android:imeOptions="actionNext" >

                <requestFocus />
            </EditText>
                        
			<Button
			    android:id="@+id/main_show_settings"
			    android:layout_width="40dp"
			    android:layout_height="40dp"
			    android:layout_marginBottom="-20dp"
			    android:layout_marginLeft="-35dp"
			    android:layout_marginTop="20dp"
			    android:background="@drawable/arrow_down" />

        </LinearLayout>

        <GridView
            android:id="@+id/main_grid_museus"
            style="@layout/view_griditem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:numColumns="1" >
        </GridView>

    </LinearLayout>
        
    <include
        android:id="@+id/main_search_settings"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:background="#00000000"
        android:visibility="gone"
        layout="@layout/view_search_menu" />

    	<!-- 
    		Per editar el layout original cal eliminar les lines de layout, background i visibility;
    		ja que efecten directament al layout i fan que no es vegi.
    		Cal tornar-les a posar per correr l'aplicació.
    	 -->
		<!-- 
        android:layout_marginTop="50dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:background="#00000000"
        android:visibility="gone"
        -->

    <include
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        layout="@layout/view_load_layout" />
    
</FrameLayout>
